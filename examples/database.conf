%{
Конфигурация базы данных
Описание параметров подключения
%}

db_port: 5432;
max_pool: 50;

-- Параметры подключения
connection: ([
  host: 127,
  port: ${db_port},
  pool_size: ${max_pool},
  settings: ([
    timeout: 60,
    retry: 3
  ])
]);
