-- Конфигурация веб-сервера

port: 8080;
timeout: 30;

-- Настройки сервера
server_config: ([
  host: 80,
  max_connections: 100,
  ssl_enabled: 1
]);

-- Использование констант
active_port: ${port};
